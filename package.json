{
  "name": "nuxt-ichibanboshi-seikyu",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "一番星 燃料サーチャージ請求システム (確認・計算・表示専用)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
